@model AppSysoHelp.Models.Contratos
@{
    var contrato = Model;
}
<div class="row">
    <div class="col-sm-12 mt-5">
        <div class="iq-card">
            <div class="iq-card-body profile-page p-0">
                <div class="profile-header">
                    <div class="profile-info p-4">
                        <div class="row">
                            <div class="col-sm-12 col-md-6">
                                <div class="user-detail pl-5">
                                    <div class="d-flex flex-wrap align-items-center">
                                        <div class="profile-img pr-4">
                                            <img src="~/images/helpdesk/_logo.png" alt="profile-img" class="avatar-130 img-fluid">
                                        </div>
                                        <div class="profile-detail">
                                            <h5>@Model.DescricaoContrato</h5>
                                            <h6>Cliente: @Model.FkCliente.NomeCliente</h6>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-sm-12 col-md-6">
                                <ul class="nav nav-pills d-flex align-items-end float-right profile-feed-items p-0 m-0">
                                    <li>
                                        <a class="nav-link active" data-toggle="pill" href="#profile-feed">@Model.PontosContratados</a>
                                    </li>
                                    <li>
                                        <a class="nav-link" data-toggle="pill" href="#profile-activity">@Model.Valor.ToString("C2")</a>
                                    </li>
                                    <li>
                                        <a class="nav-link" data-toggle="pill" href="#profile-friends">@Model.FkPlataforma.NomePlataforma</a>
                                    </li>
                                </ul>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="iq-card iq-card-block iq-card-stretch iq-card-height">
                <div class="iq-card-header d-flex justify-content-between">
                    <div class="iq-header-title">
                        <h6 class="card-title">Licenças</h6>
                    </div>
                    <div class="iq-card-header-toolbar d-flex align-items-center">
                        <button type="button" data-toggle="modal" data-target="#cadastroDeLicenca" class="btn dark-icon btn-primary mb-3">Adicionar <i class="ri-add-box-line"></i></button>
                    </div>
                </div>
                <div class="iq-card-body">
                    <div class="table-responsive">
                        <table class="table mb-0 table-borderless">
                            <thead>
                                <tr>
                                    <th scope="col">#</th>
                                    <th scope="col">Descricao</th>
                                    <th scope="col">Urlacesso</th>
                                    <th scope="col" class="text-right">Data Ativacao</th>
                                    <th scope="col" class="text-center">Status</th>
                                    <th scope="col">Ações</th>

                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model.Licencas)
                                {
                                    <tr>
                                        <td>@item.LicencaId.ToString().PadLeft(6,'0')</td>
                                        <td>@item.Descricao</td>
                                        <td>@item.Urlacesso</td>
                                        <td class="text-right">@item.DataAtivacao.ToString("dd/MM/yyyy")</td>
                                        <td class="text-center"><div class="badge badge-pill iq-bg-success"> ATIVO </div></td>
                                        @*   <td class="text-center"><div class="badge badge-pill iq-bg-danger">Past Due</div></td> *@
                                        <td>
                                            <i class="ri-ball-pen-fill text-success pr-1"></i>
                                            <i class="ri-delete-bin-5-line text-danger"></i>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>

        </div>
    </div>
</div>

<partial name="_ModalCadastrarLicenca.cshtml" />
